//Submit username
const textarea = document.getElementById("usernameBox");
const button = document.getElementById("submitButton");

button.addEventListener("click", () => {
    const textarea = placeholder.document.getElementById("usernameBox");
    textarea.value = "";
});
